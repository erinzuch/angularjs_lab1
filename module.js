"use strict";
// Defining a module
angular.module("todoApp", []); // the array can contain dependencies 

// Why do we need a module? This is our structure that we are going to assign pieces of our application to